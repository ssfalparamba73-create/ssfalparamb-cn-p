/**
 * Phase gate for member-facing payment surfaces.
 *
 * Keep the payment routes and components intact. Change this to `true` only after
 * the reviewed member payment source-of-truth phase is ready for release.
 */
export const MEMBER_PAYMENTS_ENABLED = false;
