# Supabase Schema & Migrations

This folder contains the database schema, migrations, RLS policies, RPC functions, and storage bucket definitions for the system.

## Usage
Apply these migrations to your Supabase project sequentially to setup the backend schema for a new unit/committee.

Do not edit migrations that have already been applied. Instead, create a new migration file.
